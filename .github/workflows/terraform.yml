name: 'Initalizalize multiple Modules'

on: pull_request

jobs:
  job1:
    runs-on: ubuntu-latest
    # strategy:
    #   matrix:
    #     modules: [one, two, three]
    steps:
      - run: ls
      - run: touch file.txt
  job2:
    runs-on: ubuntu-latest
    steps:
      - run: ls
